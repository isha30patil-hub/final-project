# Backend base URL the frontend talks to (HTTP; the WebSocket URL is derived from it).
# Must start with VITE_ to be visible in the browser. Restart `npm run dev` after editing.
VITE_API_URL=http://127.0.0.1:8000
